# Frontend (opciono) — kratki opis

Za prvu kontrolnu tačku frontend je opcionalan. Ako pravite, možete dodati jednostavnu React ili statičku stranicu koja poziva `GET /api/attractions`.

Predlog:
- `frontend/index.html` sa fetch pozivom ka `http://localhost:3000/api/attractions` i prikazom u tabeli.

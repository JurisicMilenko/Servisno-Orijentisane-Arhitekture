{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "BlogDatabase": "Host=${DATABASE_HOST};Port=${DATABASE_PORT};Database=${DATABASE_SCHEMA};Username=${DATABASE_USERNAME};Password=${DATABASE_PASSWORD}"
  }
}

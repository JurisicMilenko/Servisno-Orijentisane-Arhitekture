# Blog service

Simple blog microservice (stub) with basic post endpoints.

Run:

```bash
cd blog
npm install
npm start
```

Default port: 3002

Endpoints:
- GET /api/blog
- GET /api/blog/:id
- POST /api/blog

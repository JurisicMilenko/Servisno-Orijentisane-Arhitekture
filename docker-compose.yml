version: '3.8'

services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: soa_demo
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
      # initialize DB with schema from backend
      - ./backend/data/schema.sql:/docker-entrypoint-initdb.d/schema.sql:ro
    networks:
      - soa-net

  mongodb:
    image: mongo:8.0
    environment:
      MONGO_INITDB_DATABASE: tours_db
    volumes:
      - mongo_data:/data/db
    networks:
      - soa-net
    ports:
      - '27017:27017'

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      NODE_ENV: production
      PORT: 3000
      PGHOST: postgres
      PGUSER: postgres
      PGPASSWORD: postgres
      PGDATABASE: soa_demo
      PGPORT: 5432
      JWT_SECRET: change-me-in-prod
    volumes:
      - ./backend/data:/usr/src/app/data
    depends_on:
      - postgres
    networks:
      - soa-net

  stakeholders:
    build:
      context: ./stakeholders
      dockerfile: Dockerfile
    environment:
      NODE_ENV: production
      PORT: 3001
    networks:
      - soa-net

  tours:
    build:
      context: ./tours
      dockerfile: Dockerfile
    environment:
      NODE_ENV: production
      PORT: 3002
      MONGO_URI: mongodb://mongodb:27017/tours_db
      JWT_SECRET: change-me-in-prod
    depends_on:
      - mongodb
    networks:
      - soa-net

  blog:
    build:
      context: ./blog
      dockerfile: Dockerfile
    environment:
      NODE_ENV: production
      PORT: 3003
    networks:
      - soa-net

  gateway:
    build:
      context: ./gateway
      dockerfile: Dockerfile
    environment:
      NODE_ENV: production
      PORT: 4000
      AUTH_SERVICE_URL: http://backend:3000
      ATTRACTIONS_SERVICE_URL: http://backend:3000
      STAKEHOLDERS_SERVICE_URL: http://stakeholders:3001
      TOURS_SERVICE_URL: http://tours:3002
      BLOG_SERVICE_URL: http://blog:3003
    ports:
      - '4000:4000'
    depends_on:
      - backend
      - stakeholders
      - tours
      - blog
    networks:
      - soa-net

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - '8080:80'
    environment:
      API_BASE: http://localhost:4000
    networks:
      - soa-net

volumes:
  postgres_data:
  mongo_data:

networks:
  soa-net:
    driver: bridge
